---
// Properties of each service center
export type Props = {
  servicecenters: {
    LocId: number;
    LocName: string;
    LocNum: number;
    Address: string;
    City: string;
    State: string;
    Zipcode?: string;
    PhoneNumber: string;
    FaxNumber: string;
    LocEmail: string;
    Hours: string;
    StoreManager: string;
    AreaManager: string;
  }[];
};

const { servicecenters } = Astro.props;
---

<!-- Render a list of service centers -->
<ul class="mt-4 ms-0.5 ">
  {servicecenters.map((servicecenter) => (
    <!-- List item for each service center -->
    <li class="relative rounded-md shadow-md my-2 py-0 mx-0 transition ease-in duration-150 transform hover:-translate-y-1 hover:scale-100 ">
      <!-- Direct link to the service center page -->
      <a href={`/servicecenter/${servicecenter.LocId}`} class="block px-4 py-4 text-primary-content rounded-lg hover:bg-neutral-content hover:rounded-md hover:border-transparent hover:text-primary-content transition-colors">
        <div class="flex items-center justify-between">
          <div class="text-xl uppercase font-medium tracking-wide text-secondary-content">
            {servicecenter.LocName}
          </div>
          <span class="text-sm font-semibold text-gray-500">
           <!-- Store Card Possibly Goes Here -->
          </span>
        </div>
      </a>
    </li>
  ))}
</ul>

<style>
  li:nth-child(odd) {
    background-color: #075985;
  }
  li:nth-child(even) {
    background-color: #398AB9;
  }
</style>