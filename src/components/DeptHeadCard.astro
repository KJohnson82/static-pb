---
// Define properties for each head card
type Props = {
  headCard: {
    title: string;
    subtitle1: string;
    address: string;
    city: string;
    state: string;
    zip: string;
    subtitle2: string;
    phone: string;
    email: string;
    subtitle3: string;
    phone2: string;
    // phone3: string;
  }[];
};
// Extract the headCard data from props
const { headCard } = Astro.props;
---

<!-- // Render the head cards -->
<!-- Render head cards based on provided data -->
<div class=" bg-gradient-to-tl from-cyan-600 to-sky-800 text-primary-content text-opacity-90 box-decoration-clone grid grid-cols-1 grid-rows-1 w-dvw h-48 ">
  {headCard.map((card) => (
    <!-- Iterate over each card in headCard array and create a div for each card -->
    <div class=" grid grid-cols-1 border-x-[3px] border-y-[3px] border-transparent rounded-md pt-0 pb-0 mb-0   ">
        <section class=" flex items-center justify-center text-center text-wrap font-light uppercase text-3xl divide-y-[1px] tracking-wide">
        <h2 class=" my-0  text-center text-wrap font-light uppercase text-3xl divide-y-[1px] pt-4 px-2 py-0 tracking-wide  " >
        {card.title} <!-- Display the card title -->
        </h2>
        </section>
      <!-- Divider line -->
      <div class="w-lvw p-0 pb-0 m-0 border-b-2 border-slate-100 border-opacity-20  "></div>
      <!-- Sections for address, contact, and help desk information -->
      <!-- Address section -->
          {/* <section class="address-section pb-1">
          <!--<p>{card.subtitle1}</p> -->
          <div class="w-svw text-center tracking-wide align-middle pt-2 py-2 border-b-2 border-slate-100 border-opacity-20">
            <p class="text-base/5 ">{card.address} {card.city} {card.state} {card.zip}</p>
          </div>
        </section> */}
        <table class=" table-fixed border-collapse">
        <tbody class=" mt-10">
          {/* <tr class=" text-base border border-b-2 border-t-0 border-l-0 border-r-0 text-center align-middle py-1.5 border-slate-100 border-opacity-20">
              <td class=" pt-2 py-1.5 text-center align-middle w-auto " colspan="2" >{card.address} {card.city} {card.state} {card.zip}</td>
          </tr> */}
          <tr class=" text-sm border border-b-2 border-t-0 border-l-0 border-r-0 align-middle py-1.5 border-slate-100 border-opacity-20 ">
            <td class="pe-0 ps-10 py-1.5 border border-b-2 border-r-2 border-t-0 border-x-0 w-1/2 p-0 m-0 tracking-wide uppercase  border-slate-100 border-opacity-20 border-l-0  ">{card.subtitle3}</td>
            <td class="ps-2 py-1 text-sm text-left border border-b-2 border-t-0 border-x-0 w-1/2 p-0 m-0 tracking-wide capitalize  border-slate-100 border-opacity-20 border-l-0   ">{card.phone2}</td>
          </tr>
          <tr class=" text-sm">
            <td class="pe-0 ps-10 py-1 border border-b-2 border-r-2 border-t-0 border-x-0 w-1/2 p-0 m-0 tracking-wide uppercase  border-slate-100 border-opacity-20 border-l-0 ">{card.subtitle1}</td>
            <td class="ps-2 py-1 text-left  text-wrap border border-b-2 border-t-0 border-x-0 w-1/2 p-0 m-0 tracking-wide lowercase border-slate-100 border-opacity-20 border-l-0 " >{card.phone}</td>
          </tr>
          <tr class=" text-sm ">
            <td class=" pe-1 ps-4 py-1 text-center border border-b-0 border-r-0 border-t-0 border-x-0 p-0 m-0 tracking-wide lowercase  border-slate-100 border-opacity-20 border-l-0" colspan="2" >{card.email}</td>
            {/* <td class=" pe-0 ps-10 py-1 border border-b-0 border-r-2 border-t-0 border-x-0 w-1/2 p-0 m-0 tracking-wide uppercase  border-slate-100 border-opacity-20 border-l-0" >{card.subtitle3}</td>
            <td class=" ps-2 py-1 text-left text-sm border border-b-0 border-t-0 border-x-0 w-1/2 p-0 m-0 tracking-wide lowercase border-slate-100 border-opacity-20 border-l-0">{card.phone2}</td> */}
          </tr>
        </tbody>
      </table>
        {/* <!-- Contact section -->
        <section class="contact-section h-2/5 ">
          <div class="grid grid-cols-2 divide-x-2 divide-slate-100 divide-opacity-20 gap-y-10 py-1.5 ">
        <!-- Column 1 -->
        <div class=" text-center align-middle pe-2 ">
          <p class="font-normal border-b-2 uppercase tracking-wider border-slate-100 border-opacity-20 pb-0">{card.subtitle1}</p>
          <p class="pt-2 pb-0 text-base ">{card.phone}</p>
        </div>
        <!-- Column 2 -->
        <div class=" text-center align-middle ps-2 ">
          <p class="font-normal border-b-2 border-slate-100 border-opacity-20 uppercase tracking-wider pb-0 ">{card.subtitle2}</p>
          <p class="pt-2 pb-0 ps-0 text-base " >{card.phone2}</p>
        </div>
      </div>
        </section>
        <div class="w-lvw p-0 pb-0 -mt-1 border-t-2 border-slate-100 border-opacity-20  "></div>
        <!-- Help desk section -->
        <section class=" flex items-center justify-center text-center text-nowrap h-1/5  ">
          <div class="  ">
            <div class="text-base font-medium text-center align-middle py-1 ps-2 pe-2 -mt-0 mb-0 uppercase tracking-wide">{card.subtitle3}<span class="font-normal lowercase text-[15px] truncate "> {card.email}</span></div>
      </div>
        </section> */}
    </div>
  ))}
</div>


<style>
  /* Styles for dividers */
  .divider {
    margin-top: 0;
    margin-bottom: 3%;
  }
</style>
